{% extends 'base.html.twig' %}

{% block body %}
    <form method="post" data-turbo="false">
        {% if error %}
            <div class="alert alert-danger">
                {{ error.messageKey|trans(error.messageData, 'security') }}
            </div>
        {% endif %}

        <link rel="stylesheet" href="{{ asset('styles/login/styles.css') }}">

        <h1 class="h3 mb-3 font-weight-normal">Connexion</h1>
        <label for="username">Login :</label>
        <input type="text" value="{{ last_username }}" name="_username" id="username" class="form-control" autocomplete="username" required autofocus><br>
        <label for="password">Mot de passe :</label>
        <input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required><br>

        {#
            Décommentez cette section et ajoutez une option remember_me dans votre firewall pour activer la fonctionnalité "se souvenir de moi".
            Voir https://symfony.com/doc/current/security/remember_me.html

            <div class="checkbox mb-3">
                <input type="checkbox" name="_remember_me" id="_remember_me">
                <label for="_remember_me">Remember me</label>
            </div>
        #}

        <button class="btn btn-lg btn-primary" type="submit">
            Connexion
        </button><br>

        <a href="{{ path('inscription') }}" class="btn btn-primary">Inscription</a>
    </form>
{% endblock %}
